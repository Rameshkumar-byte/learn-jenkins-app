pipeline {
    agent any

    stages {
        stage('Build') {
              
            steps {
              echo "This is build from git"
              dir("${WORKSPACE}/.github"){
              sh 'ls -l'
              }
                
              
            }
        }
    }
}
